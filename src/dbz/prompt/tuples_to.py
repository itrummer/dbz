import os


def to_tuple_column(rows):
    """ Transform each row into a tuple.
    
    Args:
        rows: a list of rows where each row is a list
    
    Returns:
        one column (which <DataInstructions>) with tuples
    """