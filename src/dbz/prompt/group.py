def assign_ids(columns):
    """ Assign IDs for unique value combinations.
    
    Args:
        columns: a list of columns (each column <DataInstructions>).
    
    Returns:
        column with assigned values (the column <DataInstructions>)
    """